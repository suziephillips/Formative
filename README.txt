Created a new environment r-env-form within the formative directory, saved this as r-environment-formative.fml into this directory.

Active the environment: conda activate r-env-form

Initialise git: git init

Create .gitignore file: nano .gitignore

Check git status and folder told to ignore should not be in the list

Add files to git: git add .

Commit the files and add a title

